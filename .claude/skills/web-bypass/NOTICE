web-bypass
==========

This skill vendors the `engine/` and `references/` directories from:

    fivetaku/insane-search v0.4.0
    https://github.com/fivetaku/insane-search
    Copyright (c) 2026 fivetaku
    Licensed under the MIT License (see LICENSE)

Modifications by 이림 (Do Better Things) for use within ~/.claude/skills/:

  - Skill renamed to `web-bypass` for naming consistency with `web-crawler-ocr`
    (engine internal module name remains `engine`, unchanged).
  - Added `scripts/fetch.sh` wrapper providing 24h result caching
    (`~/.cache/web-bypass/`) and `--save-to-pkm` markdown export.
  - Added `scripts/install.sh`, `scripts/sync-upstream.sh`,
    `scripts/requirements.txt`, `SKILL.md`, `README.md`.
  - The `engine/` and `references/` directories are vendored verbatim and
    must not be edited locally; use `scripts/sync-upstream.sh` to refresh.

Vendored commit and timestamp are recorded in `upstream.txt`.
